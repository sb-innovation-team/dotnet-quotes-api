using Microsoft.AspNetCore.Routing;

namespace socialbrothers_quotes_api {
    public static class Routes {
        public static void Configure(this IRouteBuilder routes) {
            // Quotes
        }
    }
}